pipeline {
    agent any

    stages {
        stage('Code Checkout') {
            steps {
                git branch: 'main', credentialsId: 'github_hp', url: 'https://github.com/harshaprakash100/ip_app.git'
            }
        }

        stage('Build1') {
            steps {
                sh 'ls -lrt'
            }
        }

        stage('Build2') {
            steps {
                sh 'ls -lrt'
            }
        }
    }
}